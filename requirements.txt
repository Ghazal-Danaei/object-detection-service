# --- Core ML / inference ---
ultralytics>=8.1.0        # YOLOv8 model + ONNX export (pulls in torch, torchvision, opencv)
onnx>=1.15.0
onnxruntime>=1.17.0       # use onnxruntime-gpu instead for CUDA inference
numpy>=1.24.0
pillow>=10.0.0

# --- API / serving ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9   # required by FastAPI for file uploads

# --- Tests ---
pytest>=8.0.0
httpx>=0.27.0             # used by FastAPI's TestClient
